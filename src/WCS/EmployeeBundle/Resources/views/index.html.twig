{% extends '::base.html.twig' %}
{% block body %}
    <div class="container-fluid">

        <div class="commande">
            <a href="{{ path('wcs_employee_schools', {'type_activity': 'cantine'}) }}">
                <button class="col-sm-offset-4 col-md-offset-4">
                    <i class="fa fa-cutlery fa-2x"></i>
                    Cantine
                </button>
            </a>
        </div>

        {% if wcs_group_tap_garderie  %}
            <div class="commande">
                <a href="{{ path('wcs_employee_schools', {'type_activity': 'tap_garderie'}) }}">
                    <button class="col-sm-offset-4 col-md-offset-4">
                        <i class="fa fa-home fa-2x"></i>
                        TAP/Garderie
                    </button>
                </a>
            </div>

            {% else %}
            <div class="commande">
                <a href="{{ path('wcs_employee_schools', {'type_activity': 'tap'}) }}">
                    <button class="col-sm-offset-4 col-md-offset-4">
                        <i class="fa fa-home fa-2x"></i>
                        TAP
                    </button>
                </a>
            </div>

            <div class="commande">
                <a href="{{ path('wcs_employee_schools', {'type_activity': 'garderie_matin'}) }}">
                    <button class="col-sm-offset-4 col-md-offset-4">
                        <i class="fa fa-home fa-2x"></i>
                        Garderie matin
                    </button>
                </a>
            </div>

            <div class="commande">
                <a href="{{ path('wcs_employee_schools', {'type_activity': 'garderie_soir'}) }}">
                    <button class="col-sm-offset-4 col-md-offset-4">
                        <i class="fa fa-home fa-2x"></i>
                        Garderie soir
                    </button>
                </a>
            </div>
        {% endif %}
    </div>

{% endblock %}

{% block footer %}{% endblock %}
